<%= simple_form_for(@category) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">

    <%= f.association :posts %>
    ##something is wrong with the above not sure what- trying to have a list of posts but throwing an error
    <%= f.input :name %>

  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
